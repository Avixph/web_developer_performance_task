# from django.contrib import admin
# from django.urls import path, re_path
# from server_app import views

# urlpatterns = [
#     # path('admin/', admin.site.urls),
#     re_path(r'^api/notes$', views.astra_note_list),
#     re_path(r'^api/notes/(?P<pk>[0-9]+)$', views.note_detail),
#     re_path(r'^api/notes/completed$', views.note_completed_list),
# ]
